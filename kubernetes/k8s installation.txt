docker image inspect <container name>
default.svc.cluster.local svc.cluster.local cluster.local

step1: 
	execute this commands on azureportal terminal
	---
	export MY_RESOURCE_GROUP_NAME="myAKSResourceGroup"
	export REGION="eastus"
	export MY_AKS_CLUSTER_NAME="myAKSCluster"
	export MY_DNS_LABEL="mydnslabel" az group create \
    

step2:
	--name $MY_RESOURCE_GROUP_NAME \
    	--location $REGION
step3:
	az aks create \
    	--resource-group $MY_RESOURCE_GROUP_NAME \
    	--name $MY_AKS_CLUSTER_NAME \
    	--node-count 1 \
    	--generate-ssh-keys \
    	--node-vm-size Standard_B2ms
step4:
	az aks get-credentials \
    	--resource-group $MY_RESOURCE_GROUP_NAME \
    	--name $MY_AKS_CLUSTER_NAME